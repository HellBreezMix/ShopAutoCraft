-- /home/.shrc
-- Выполняется автоматически при каждом старте оболочки OpenOS (то есть при
-- каждой загрузке компьютера) — благодаря этому crafter запускается сам,
-- без необходимости набирать команду вручную. Это же нужно и для того,
-- чтобы аварийная авто-перезагрузка при переполнении RAM (см. crafter.lua)
-- сама поднимала программу обратно после reboot.
crafter
